# 摄像头记录：JQ-CAM12-720P

## 0. 决策记录（2026-08-28）

- **路线已定：路线 A** —— 转用 ESP-IDF，用 esp-iot-solution 的 `usb_host_uvc` 组件直连摄像头（用户接受 Arduino → ESP-IDF）。
- **摄像头用途**：① 摄像头巡线（替代已停用的四路红外巡线）；② 后续识别红色小球。
- **引脚调整**（[v26] 已改入 .ino）：
  - 超声波 `TRIG: 19 → 6`、`ECHO: 20 → 7`
  - GPIO19/20 让位给摄像头 USB `D-`/`D+`（S3 原生 USB 引脚，硬件固定不可改）
  - 四路红外（GPIO4~7）停用退役
- **注意事项**：
  - 红外板若仍接在 6/7 上，会与超声 TRIG(6) 输出顶牛，**重新上电前必须拔除**。
  - 摄像头 4Pin 的 5V/GND 必须接车上 5V 稳压输出并共地，不能只接 D±。
  - 板子带 Octal PSRAM（arduino-cli fqbn `PSRAM=opi`，16MB Flash），满足 `usb_host_uvc` 建议。
  - 待确认：COM7 是原生 USB-CDC（19/20）还是独立 UART 桥——决定摄像头接上后还能否烧录/看串口。
- **性能预期**：S3 上不做 720p@30 全帧解码；从 640×480 或更低分辨率、10~15fps 起步，控制环（编码器/电机）与 USB+图像分核运行（延续 v25 的 core0/core1 分工）。

## 1. 从图片确认到的信息

| 项目 | 图片标注 |
|---|---|
| 型号 | `JQ-CAM12-720P-V1`（尺寸图底部标注） |
| 板上版本字样 | 竖排丝印疑似 `CAM12-720P-V2`，需以后查看实物丝印确认 |
| 类型 | USB 三合一摄像头：摄像头 + 麦克风 + 喇叭 |
| 分辨率 | 1280 × 720（720P） |
| 视场角 | 104° |
| 镜头 | 4P+IR |
| 芯片/感光片 | 1/4 英寸 |
| 焦距 | F=1.5 |
| 光圈 | F=1.9 |
| 畸变 | < 1% |
| 外形尺寸 | 约 22 mm × 30 mm；侧向厚度图约 11.1 mm |
| 工作电压 | 5 V |
| 接口 | 4 针摄像头接口 |

## 2. 线缆接口

图片右下角明确标出了 USB 线的四个信号：

- `5V`：电源正极
- `GND`：电源地
- `D+`：USB 差分数据正
- `D-`：USB 差分数据负

颜色不能只凭图片推断，实际接线应以线缆上的标签、卖家接线表或万用表确认。5 V 电源必须与设备的 USB 供电要求匹配，不能把 D+/D- 当作普通 GPIO 使用。

## 3. 它能做什么

摄像头本身只负责采集图像；“巡线”和“识别小球”需要上位机或 ESP32-S3 对图像进行处理。

### 摄像头巡线

建议流程：

1. 摄像头采集画面。
2. 裁剪画面下方的一条感兴趣区域（ROI），因为黑线通常位于地面区域。
3. 转灰度并二值化，分离黑线和地面。
4. 计算黑线中心相对于画面中心的偏差。
5. 根据偏差控制小车左右轮速度。

摄像头巡线与现在的四路红外巡线是两套输入方式。切换到摄像头巡线时，应先保留原红外代码作为备用，不要同时让两套控制逻辑写电机。

### 小球识别

如果小球颜色明显，优先采用颜色阈值：

1. 将图像转换到 HSV 或类似颜色空间。
2. 设定小球颜色范围。
3. 进行去噪和连通区域筛选。
4. 找到最大候选区域，计算中心点和面积。
5. 用中心点判断小球在画面左、右或中间；用面积或半径粗略判断距离。

如果小球颜色、光照或背景变化很大，再考虑模板匹配或机器学习模型。对 ESP32-S3 而言，先做低分辨率、低帧率和简单颜色识别更现实。

## 4. 与 ESP32-S3 连接前必须确认

这款模块标注为 USB 摄像头形态，但仅凭图片不能确认它是否能被 ESP32-S3 直接识别。需要确认：

- 模块是否输出标准 USB UVC 视频。
- ESP32-S3 使用哪个 USB 口作为 USB Host。
- 开发板是否把 USB D+/D- 引脚引出。
- 是否需要 USB OTG 转接线、扩展板或额外 5 V 供电。
- 摄像头的实际电流是否超过开发板 5 V 供电能力。
- Arduino 库或 ESP-IDF 驱动是否支持该摄像头的 USB/UVC 协议。

普通 ESP32-S3 开发板的 USB 口通常用于下载和串口，不一定能直接当 USB Host 给摄像头使用。若不能直接连接，常见方案是使用带 USB Host 的扩展板，或改用带并行 DVP 接口的 OV2640/OV5640 摄像头模块。

## 5. 建议的首次测试顺序

1. 先把摄像头接到电脑，而不是直接接小车。
2. 在 Windows“相机”应用或设备管理器中确认是否出现视频设备。
3. 测试 1280×720 画面、帧率和视角。
4. 检查摄像头线缆标签，记录 5V、GND、D+、D- 的实际颜色。
5. 再确认 ESP32-S3 的 USB Host 接口和软件支持。
6. 最后单独做“摄像头取图”，确认稳定后再接入巡线控制。

## 6. 适合课程项目的分阶段方案

### 阶段 A：电脑验证

用电脑读取摄像头画面，完成黑线 ROI 和小球颜色识别。此阶段不连接电机，便于调试图像算法。

### 阶段 B：ESP32-S3 取图验证

只验证 ESP32-S3 能否枚举摄像头、读取一帧图像并输出基本信息。先不控制小车。

### 阶段 C：摄像头巡线

将图像中的黑线偏差转换为电机差速，低速测试。必须设置失去图像或识别失败时的安全动作，例如停止，而不是继续高速运行。

### 阶段 D：小球识别

先输出小球的左/中/右位置和面积，再决定小车动作。不要一开始就把识别和避障、巡线全部合并。

## 7. 当前记录的限制

- 型号版本存在 `V1` 与疑似 `V2` 的图片信息冲突，需以实物丝印或包装确认。
- 图片没有给出 ESP32-S3 专用接线表，也没有证明该模块可被当前开发板直接 USB Host 识别。
- 图片中的线色不应作为可靠接线依据。


## 8. 阶段 B 实测记录（2026-08-28，已通过）

测试工程 `esp32_uvc_test/`（ESP-IDF v5.4.4 + usb_host_uvc 2.5.2），接线 D-=GPIO19 / D+=GPIO20，5V/GND 取自车上 5V。

**实测支持的格式（仅 MJPEG，无 YUY2）：**

| 档位 | 分辨率 | 标称帧率 |
|---|---|---|
| 0 | 1280x720 | 15 fps |
| 1 | 800x480 | 20 fps |
| 2 | 640x480 | 25 fps |
| 3 | 480x320 | 25 fps |
| 4 | 480x854（竖屏） | 25 fps |

**720p 实测取流**：14.0~14.2 fps（标称15），平均 39~40 KB/帧，带宽 4.6~4.7 Mbit/s，连续 7 个 5s 窗口零丢帧零报错。

**关键坑（已解决）**：该模组是 UVC+UAC 复合设备，配置描述符超过 IDF 默认的 256 字节枚举缓冲（实测 512 也不够），必须设 `CONFIG_USB_HOST_CONTROL_TRANSFER_MAX_SIZE=2048`，否则报 `Configuration descriptor larger than control transfer max length`，枚举失败。

**对巡线的启示**：720p 只有 15fps 且解码量大；正式工程建议用 640x480@25 或 800x480@20 档位，帧率更高、解码更快，巡线够用。
